import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react' // plugins usados

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()]
})

// arquivo de config
